# Сборка на Mac

Открой `HandARBrowser.xcodeproj` в Xcode и выбери target `HandARBrowser`.

Проект рассчитан на iOS 17.0+ и собирается под arm64 iPhoneOS.

Перед запуском на iPhone нужно дать приложению доступ к камере. В AR-режиме телефон должен быть установлен горизонтально в VR-держатель.
